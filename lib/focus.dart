import 'package:flutter/material.dart';

class focus_view extends StatelessWidget {
  const focus_view({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}